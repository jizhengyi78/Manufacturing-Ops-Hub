# 系统评测与验收方案

## 制造业多 Agent 生产运维数字助手

---

## 一、多维验收指标与通过阈值

### 1.1 功能验收

| 编号 | 验收项 | 通过标准 | 验证方式 |
|------|--------|---------|---------|
| F-01 | P0 核心场景全流程 | 10/10 条预定义场景全部跑通 | End-to-End 测试脚本 |
| F-02 | 文档摄入 | PDF/Word/Excel 上传后 5 分钟内可检索 | 上传后查检索接口 |
| F-03 | 流式对话 | 输入到首字 SSE 事件 < 2s | 压测工具计时 |
| F-04 | 角色权限 | 6 个角色场景 RBAC 校验全部正确 | 自动化测试脚本 |
| F-05 | 工单同步 | MES 工单创建→回调→状态更新闭环 | 模拟 MES 回调 |
| F-06 | 离线切换 | 断网后 30s 内自动切离线，基础问答可用 | 网络断开测试 |
| F-07 | 降级兜底 | LLM全挂时 10 条核心报警码 100% 规则命中 | Mock LLM故障 |
| F-08 | 知识沉淀 | 案例自动生成→审核→入库→可检索 | 端到端测试 |
| F-09 | 前端适配 | 对话界面 PC/手机浏览器正常渲染、流式正常 | 手动验收 |

### 1.2 质量指标

| 指标 | 目标阈值 | 测量方法 | 评测数据集 |
|------|---------|---------|-----------|
| 根因匹配准确率 | ≥ 85% | 诊断Agent输出 vs 实际维修确认 | fault_cases.json (30条) |
| SOP 步骤完整度 | ≥ 90% | 回答步骤 / 标准SOP步骤数 | sop_qa.json (50条) |
| 设备型号匹配正确率 | ≥ 95% | NER提取型号 vs 实际型号 | 综合测试集 |
| 报警码解释准确率 | ≥ 90% | 回答含义 vs 标准含义 | alarm_cases.json (30条) |
| 幻觉引用率 | ≤ 5% | 引用不存在 / 总引用数 | 自动回溯检索库校验 |
| 回答相关性 (RAGAS) | ≥ 0.85 | RAGAS Answer Relevancy | 自动化评测 |
| 上下文召回率 (RAGAS) | ≥ 0.80 | RAGAS Context Recall | 自动化评测 |
| 忠实度 (RAGAS) | ≥ 0.85 | RAGAS Faithfulness | 自动化评测 |
| 首次解决率 | ≥ 60% | 👍 / 总对话数 | 在线埋点统计 |

### 1.3 性能指标

| 指标 | 目标阈值 | 测量工具 | 压测条件 |
|------|---------|---------|---------|
| 对话首字延迟 (P95) | < 2s | Locust / k6 | 50 并发 |
| 对话完整延迟 (P95) | < 15s | Locust / k6 | 50 并发 |
| 系统 P99 延迟 | < 3s | 同上 | 50 并发 |
| 系统 P99 延迟 (峰值) | < 5s | 同上 | 100 并发 |
| 接口成功率 | ≥ 99.5% | 同上 | 50 并发 × 10min |
| Milvus 单查询延迟 (P99) | < 200ms | 内置埋点 | 混合检索场景 |
| ES 单查询延迟 (P99) | < 100ms | 内置埋点 | BM25 检索 |
| 文档索引吞吐 | > 5MB/min | 计时 | 10 份文档批量导入 |

### 1.4 安全指标

| 指标 | 目标阈值 | 测试方式 |
|------|---------|---------|
| Prompt 注入拦截率 | ≥ 99% | 25 条注入 payload 自动化回归 |
| 检索上下文注入拦截率 | ≥ 95% | 10 条污染文档检索测试 |
| RBAC 越权拦截率 | 100% | 12 条越权测试用例 (6角色×2场景) |
| 跨车间数据隔离 | 100% | A车间角色查B车间数据全拦截 |
| A2A 签名防伪造 | 100% | 错误签名请求全拒绝 |
| 敏感数据脱敏准确率 | 100% | 成本/人事数据不出现于低权限响应 |

### 1.5 韧性指标

| 指标 | 目标阈值 | 测试方式 |
|------|---------|---------|
| 离线切换时间 | < 30s | 手动断网计时 |
| 在线恢复时间 | < 60s | 网络恢复计时 |
| 规则降级覆盖率 | 核心报警码 100% | Mock LLM全挂后查询 |
| 规则降级准确率 | ≥ 95% | 人工审核降级返回SOP |
| Checkpoint 恢复成功率 | 100% | 服务重启后对话可继续 |
| 熔断后恢复成功率 | 100% (半开渐进) | Mock 连续失败触发熔断 |
| MES 工单最终一致性 | 100% (24h内) | 模拟MES故障后补偿对账 |

---

## 二、评测数据集构建标准

### 2.1 数据集总览

| 数据集 | 样本量 | 标注方式 | 覆盖范围 |
|--------|--------|---------|---------|
| fault_cases.json | 30条 | 人工标注根因+维修步骤 | 注塑/冲压/CNC 各 10 条 |
| sop_qa.json | 50条 | 人工标注标准答案+步骤列表 | 换模/点检/保养/维修全覆盖 |
| alarm_cases.json | 30条 | 人工标注报警码含义+处理流程 | 20条核心+10条扩展 |
| injection_test.json | 25条 | 注入/正常混合 | 角色冒充/指令劫持/数据窃取 |
| rbac_test.json | 12条 | 6角色×2越权场景 | 跨车间/跨密级/跨角色 |
| safety_eval.json | 10条 | 检索污染文档 | 含恶意指令的外部文档 |

### 2.2 单条数据格式

```json
{
  "id": "fault_001",
  "category": "fault_diagnosis",
  "equipment_type": "injection_molding",
  "equipment_model": "海天MA1200",
  "query": "注塑机料筒温度显示异常，实际温度和设定温度差30度",
  "context": {
    "fault_code": "HT-E-0021",
    "workshop_id": "workshop-a"
  },
  "expected": {
    "root_cause": "料筒温度传感器故障或温控器参数漂移",
    "repair_steps": [
      "检查温控器接线是否松动",
      "测量传感器电阻值（标准值100Ω±5%）",
      "若电阻异常则更换温度传感器",
      "更换后重新校准PID参数"
    ],
    "sop_reference": "SOP-HA-12",
    "key_keywords": ["传感器", "电阻", "PID"]
  },
  "eval_dimensions": ["root_cause_accuracy", "step_completeness", "model_accuracy"]
}
```

### 2.3 数据分布要求

| 维度 | 要求 |
|------|------|
| 设备类型 | 注塑 40% / 冲压 30% / CNC 30% |
| 故障难度 | 常见(60%) / 一般(30%) / 罕见(10%) |
| 查询类型 | 精确型号(30%) / 混合(40%) / 模糊语义(30%) |
| 查询长度 | 短(<20字) 30% / 中(20-50字) 50% / 长(>50字) 20% |

---

## 三、评测执行流程

### 3.1 离线评测（每次代码变更自动触发）

```
CI 触发 → 数据准备 → 检索评测 → RAGAS评测 → 领域指标评测 → 安全评测 → 报告生成
```

| 阶段 | 内容 | 耗时 |
|------|------|------|
| 1. 数据准备 | 加载评测数据集，初始化评测环境 | 1min |
| 2. 检索评测 | 对每条 query 跑混合检索，计算 Recall@5/10/20 | 5min |
| 3. RAGAS 评测 | 生成回答 → 计算 Faithfulness/Context Recall/Answer Relevancy | 10min |
| 4. 领域指标 | 根因准确率 / SOP完整度 / 型号匹配率 / 幻觉率 | 5min |
| 5. 安全评测 | 注入拦截率 / 越权拦截率 / 检索污染拦截率 | 3min |
| 6. 报告生成 | 汇总指标，与上一次基线对比，输出 Markdown 报告 | 1min |

### 3.2 集成测试（Phase 里程碑触发）

```
集成测试 → 端到端场景 → 异常路径 → 状态恢复
```

| 场景 | 验证点 |
|------|--------|
| 正常故障处理闭环 | 告警→诊断→SOP检索→工单创建→维修→质检→沉淀 |
| LLM降级路径 | Mock DeepSeek故障 → 自动降级Qwen → 规则兜底 |
| MES故障路径 | Mock MES超时 → 本地工单pending → 补偿Worker恢复 |
| 离线切换路径 | 断网 → 自动离线 → 基础问答 → 恢复在线 → 数据同步 |
| Checkpoint恢复 | 服务重启 → 从Checkpoint恢复 → 对话可继续 |
| 并发竞态 | 同设备30并发告警 → 去重合并 → 不产生重复工单 |

### 3.3 压力测试（上线前）

```
压测脚本 (Locust) → 阶梯加压 → 稳态保持 → 观察指标 → 降压
```

| 阶段 | 并发数 | 持续时间 | 观察指标 |
|------|--------|---------|---------|
| 预热 | 10 | 2min | 基线延迟 |
| 加压 | 10→50 | 5min | 延迟变化趋势 |
| 稳态 | 50 | 10min | P50/P95/P99/成功率 |
| 峰值 | 100 | 5min | 降级触发/排队情况 |
| 降压 | 100→10 | 2min | 恢复速度 |

### 3.4 安全测试（上线前）

```
注入攻击测试集 → 逐一发送 → 验证拦截 → 统计拦截率
RBAC越权测试集 → 逐角色逐场景 → 验证拦截 → 统计拦截率
检索污染测试 → 导入含注入文档 → 检索 → 验证清洗层 → 统计拦截率
```

### 3.5 灰度上线与在线验收

| 阶段 | 流量比例 | 持续时间 | 验收条件 |
|------|---------|---------|---------|
| 灰度 | 10% | 3天 | 无Critical告警，接口成功率≥99.5% |
| 放量 | 50%  | 3天 | 用户反馈满意度≥80% |
| 全量 | 100% | - | 全部P0指标达标 |

---

## 四、上线准入条件

### 4.1 必须全部通过（阻塞上线）

- [ ] P0 核心场景 10/10 全部跑通
- [ ] 根因匹配准确率 ≥ 85%
- [ ] SOP 步骤完整度 ≥ 90%
- [ ] 幻觉引用率 ≤ 5%
- [ ] Prompt 注入拦截率 ≥ 99%
- [ ] RBAC 越权拦截率 = 100%
- [ ] 50 并发下接口成功率 ≥ 99.5%
- [ ] 50 并发下 P99 延迟 < 3s
- [ ] 离线切换时间 < 30s
- [ ] 规则降级核心报警码覆盖率 = 100%
- [ ] 无高危安全漏洞（注入/越权/脱敏）
- [ ] 核心业务闭环可完整走通
- [ ] 所有评测报告已归档

### 4.2 允许降级通过（不阻塞上线，记录跟踪）

- [ ] 首次解决率 ≥ 60%（上线后持续优化）
- [ ] 非核心报警码规则覆盖率 ≥ 80%
- [ ] 100 并发 P99 < 5s
- [ ] 离线模式并发 ≥ 8 路（RTX4090）
- [ ] 检索污染拦截率 ≥ 95%

---

## 五、评测工具链

| 工具 | 用途 |
|------|------|
| RAGAS | RAG 质量评测 (Faithfulness/Context Recall/Answer Relevancy) |
| Locust | HTTP/WebSocket 压力测试 |
| pytest | 单元测试 + 集成测试 + E2E测试 |
| Pytest-benchmark | 性能基准测试 |
| Custom eval runner | 领域指标评测 (evaluation/runner.py) |
| Custom safety runner | 安全自动化回归 (evaluation/metrics/safety_eval.py) |

---

## 六、持续评测机制

```
每次 PR → CI 自动跑离线评测 → 指标与 baseline 对比 → 劣化超过5%则阻断合并

每周 → 全量评测 + 人工抽样审核 20 条对话

每月 → 更新评测数据集（新增线上真实案例）+ 重新标注
```

评测报告模板：

```markdown
## 评测报告 {date}

### 总览
| 指标 | 本次 | 上次 | 变化 |
|------|------|------|------|
| 根因准确率 | 87.2% | 85.1% | +2.1% ✅ |
| SOP完整度 | 91.5% | 90.8% | +0.7% ✅ |
| 幻觉引用率 | 3.2% | 4.1% | -0.9% ✅ |

### 不通过项
- (无)

### 关注项
- 首次解决率 58%，低于目标 60%，建议优化知识库覆盖范围
```
